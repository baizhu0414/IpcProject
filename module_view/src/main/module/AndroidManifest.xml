<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.module_view">

    <application
        android:allowBackup="true"
        android:icon="@drawable/test1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExpandableActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DecorationActivity"/>
        <activity android:name=".SimpleScrollActivity"/>
        <activity android:name=".ViewScrollActivity" />
        <activity android:name=".ViewActivity" />
    </application>

</manifest>